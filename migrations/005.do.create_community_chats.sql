CREATE TABLE IF NOT EXISTS community_chats (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    user1_id INTEGER REFERENCES community_users(id) ON DELETE CASCADE,
    user2_id INTEGER REFERENCES community_users(id) ON DELETE CASCADE
);